# Repository Guidelines

## 项目结构与模块组织
本仓库用于正式需求开发，采用标准分层：
- `docs/`：需求文档、计划、设计说明
- `src/`：核心实现代码
- `tests/`：自动化测试
- `scripts/`：辅助脚本（初始化、数据处理、发布）
- `assets/`：静态资源（图片、样例数据）
- `参考/`：外部参考资料，不作为最终交付内容

## 构建、测试与开发命令
当前未绑定单一技术栈，先使用通用命令：
- `Get-ChildItem -Recurse`：查看完整目录结构
- `rg --files`：快速列出文件
- `rg "TODO|FIXME|待办" .`：扫描待处理项
- `git status`：检查工作区改动

后续引入具体栈后，必须在 `README.md` 补充明确命令（如 `npm test`、`pytest`、`make build`）。

## 代码风格与命名规范
- 缩进默认 4 空格（除非语言规范另有要求）
- 文档文件优先 `kebab-case` 或中文语义化命名
- Python 文件使用 `snake_case`，类名使用 `PascalCase`
- 新增代码时同步引入格式化/检查工具（如 `ruff`/`black`、`eslint`/`prettier`）

## 测试规范
- 测试文件统一放在 `tests/`
- 命名建议：`test_<模块>_<行为>.py` 或 `<module>.test.ts`
- 合并前至少覆盖核心主流程与一个异常分支
- 暂无自动化测试时，需在 PR 描述中写清手工验证步骤

## 提交与合并请求规范
- 提交信息使用简洁前缀：`feat:`、`fix:`、`docs:`、`chore:`
- 单次提交只做一类改动，避免“混合提交”
- PR 必须包含：
  - 变更目的与范围
  - 影响路径（例如 `src/`、`docs/需求说明.md`）
  - 验证方式与结果
  - 涉及界面时附截图

## 安全与配置建议
- 禁止提交密钥、令牌、Cookie、私有 URL
- 使用占位符：`<API_KEY>`、`<TOKEN>`
- 本地环境变量放在 `.env.local`（已加入忽略）
